---
phase: 07-bot-settings-docs-packaging-audit
plan: 03
title: Linux and Windows Packaging Configurations and Milestone Parity
wave: 3
depends_on: [01, 02]
requirements: [CORE-06]
---

# Plan 07-03: Packaging & Parity Audit

## Goal
Mengonfigurasikan metadata installer paket native: Linux `.deb` (via `cargo-deb`) dan Windows `.msi` (via `cargo-wix`), memperbarui CI workflow, dan memvalidasi seluruh checklist requirement.

## Proposed Changes
- `desktop-gpui/crates/app/Cargo.toml`:
  - Menambahkan konfigurasi metadata `[package.metadata.deb]` dan assets bundling
- `.github/workflows/desktop-gpui.yml`:
  - Menambahkan langkah verifikasi packaging metadata
- Audit seluruh requirement v1.0 lolos tanpa gap.

## Verification
- CI workflow YAML validasi
- `cargo check --workspace` lolos
- `cargo test --workspace` lolos
