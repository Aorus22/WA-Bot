---
phase: 03-shell-theming-auth
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - desktop-gpui/crates/app/src/router.rs
  - desktop-gpui/crates/app/src/components/mod.rs
  - desktop-gpui/crates/app/src/components/titlebar.rs
  - desktop-gpui/crates/app/src/components/nav_sidebar.rs
  - desktop-gpui/crates/app/src/components/connection_banner.rs
  - desktop-gpui/crates/app/src/components/toast.rs
  - desktop-gpui/crates/app/src/views/mod.rs
  - desktop-gpui/crates/app/src/views/shell.rs
autonomous: true
requirements:
  - SET-04
  - SET-07
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Custom frameless title bar dengan drag-region dan tombol kontrol (min, max, close)
    - Navigation sidebar 1:1 web menampilkan 9 item rute lengkap dengan tooltip dan active indicator
    - Indikator koneksi WebSocket menampilkan banner reconnecting saat terputus dan menghilang saat tersambung
    - Sistem toast notification menampilkan feedback operasional di posisi top-center
  artifacts:
    - desktop-gpui/crates/app/src/components/nav_sidebar.rs
    - desktop-gpui/crates/app/src/components/connection_banner.rs
    - desktop-gpui/crates/app/src/views/shell.rs
  key_links:
    - AppShellView membungkus Root, titlebar, sidebar, router outlet, dan layer toast
---

<objective>
Membangun layout shell aplikasi (`AppShellView`), titlebar frameless dengan kontrol jendela, navigation sidebar 1:1 web, indikator koneksi WebSocket (online dot + reconnect banner), router navigasi antar halaman, dan sistem toast notification.
</objective>

<tasks>
1. Buat komponen `titlebar.rs` (drag region, window title, minimize/maximize/close buttons) dan `router.rs` yang mengelola navigasi antar route (`/chat`, `/status`, `/channels`, `/calls`, `/triggers`, `/cron`, `/webhooks`, `/documentation`, `/settings`).
2. Buat komponen `nav_sidebar.rs` vertikal lebar 68px dengan 9 item menu (Chats, Status, Channels, Calls, Triggers, Cron, Webhooks, Documentation, Settings di bagian bawah), tooltip label, dan styling aktif ber-tema.
3. Buat komponen `connection_banner.rs` untuk status WS (online vs reconnecting) dan sistem `toast.rs` yang mengintegrasikan helper push notification (`info`, `success`, `warning`, `error`) via `gpui-component`.
</tasks>
