---
phase: 01-fondasi-workspace-supervisor
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - desktop-gpui/crates/supervisor/Cargo.toml
  - desktop-gpui/crates/supervisor/src/lib.rs
  - desktop-gpui/crates/supervisor/tests/lifecycle.rs
autonomous: true
requirements:
  - CORE-02
estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Aplikasi men-spawn Go backend sebagai sidecar dan handshake port berhasil
    - Tidak ada proses orphan saat ditutup atau crash-restart (kill bersih)
    - Mode dev --no-backend --port X terhubung ke backend eksternal
  artifacts:
    - desktop-gpui/crates/supervisor/src/lib.rs
    - desktop-gpui/crates/supervisor/tests/lifecycle.rs
  key_links:
    - Supervisor parse BACKEND_PORT: dari stdout -> BackendInfo base URL -> readiness probe GET /api/settings
    - Supervisor stop() -> tidak ada wa-bot-backend tersisa di process table
---

<objective>
Crate `wabot-supervisor` men-spawn Go backend sebagai sidecar, handshake port, readiness probe, dan kill bersih tanpa orphan — port eksak `desktop/main.js` plus pola `crates/supervisor` web-term. Mencegah Pitfall #2 (orphan/zombie + port race).

Purpose: Sidecar adalah satu-satunya jembatan desktop ke backend; supervisor bocor berarti sesi ganda dan port race setelah crash.
Output: Crate supervisor GPUI-free (tokio-only) yang headless-testable di CI, siap dikonsumsi Phase 3 (boot) dan Phase 2 (base URL).
</objective>

<execution_context>
@/home/aorus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/aorus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-fondasi-workspace-supervisor/01-CONTEXT.md
@.planning/phases/01-fondasi-workspace-supervisor/01-01-SUMMARY.md
@/home/aorus/project/wa-bot/desktop/main.js
@/home/aorus/project/web-term/desktop-gpui/crates/supervisor/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Tracer: spawn satu fake backend -> handshake -> kill tanpa orphan</name>
  <files>desktop-gpui/crates/supervisor/src/lib.rs, desktop-gpui/crates/supervisor/tests/lifecycle.rs</files>
  <action>Implementasi inti crate wabot-supervisor (tokio-only, TANPA dependensi gpui): struct SpawnOptions berisi backend_path, db_path, media_path (PathBuf absolut; cwd tidak boleh berpengaruh), allowed_origins default bintang, readiness_timeout dan handshake_timeout default 15 detik. Method env_vars mengembalikan TEPAT lima pasang dari kontrak main.js: PORT dengan nilai colon-nol (ephemeral), ALLOWED_ORIGINS bintang, DB_PATH, MEDIA_PATH, TZ Asia/Jakarta — dan secara eksplisit TANPA kunci enkripsi apa pun (anti-pattern ARCHITECTURE: wa-bot tidak punya encryption key seperti WEBTERM_ENCRYPTION_KEY web-term; jangan copy field itu). Fungsi pub parse_handshake_line mem-parse substring BACKEND_PORT: diikuti digit menjadi u16 (tahan terhadap baris log lain dan prefix/suffix noise). Struct Supervisor dengan spawn (tokio process, stdin null, stdout piped untuk handshake, stderr ring buffer newest-wins kap 2000 chars, kill_on_drop true) dan stop (graceful di unix via SIGTERM lalu kill fallback; kill langsung di Windows). Test tracer memakai skrip fake backend (sh -c printf BACKEND_PORT lalu sleep) untuk membuktikan spawn→parse→ready→stop→tidak-ada-orphan dalam satu alur; asersi orphan dengan memeriksa tidak ada child tersisa (Child::try_wait / kill lalu wait). Baca file referensi web-term hanya untuk mekanika (handshake parse, stderr ring, watch channel), tulis env dari main.js.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo test -p wabot-supervisor --manifest-path desktop-gpui/Cargo.toml</automated></verify>
  <done>Satu alur spawn→handshake→kill hijau melawan fake backend; tidak ada proses tersisa setelah stop</done>
</task>

<task type="auto">
  <name>Readiness probe + status channel + CLI override --no-backend</name>
  <files>desktop-gpui/crates/supervisor/src/lib.rs</files>
  <action>Perluas crate dari tracer: enum BackendStatus (Starting, Ready, Failed berisi reason + stderr_tail, Crashed berisi exit_code) yang disebarkan via tokio watch channel agar Phase 3 bisa me-render reconnect banner; method wait_ready yang poll GET {base}/api/settings dengan timeout readiness_timeout dan transisi ke Failed membawa tail stderr saat timeout/exit-dini/path-invalid. Fungsi resolve_backend_path dengan lookup dua tingkat paritas Electron: biner packaged di resources/be/wa-bot-backend(.exe di cfg windows), fallback dev ke <repo>/wa-bot-backend(.exe) — dua tingkat saja, tanpa tebakan path lain. Porting fungsi CLI dari main.js: has_cli_flag dan parse_backend_port yang menerima nilai 1-65535 dan menolak non-digit/out-of-range sebagai error (bukan default diam-diam), plus tipe ExternalBackend { port } untuk mode --no-backend --port. Tambahkan adopt-or-kill single-instance: sebelum spawn, upaya bind probe ke port terakhir yang diketahui bila ada, dan dokumentasikan perilaku kill pada startup di doc-comment. Semua secret hanya via env, tidak pernah argv; tidak ada logging isi env secret.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo test -p wabot-supervisor --manifest-path desktop-gpui/Cargo.toml && cargo clippy -p wabot-supervisor --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated></verify>
  <done>Probe + status + CLI override teruji; clippy bersih tanpa warning</done>
</task>

<task type="auto">
  <name>Uji restart-loop + live-backend opsional + data dirs</name>
  <files>desktop-gpui/crates/supervisor/tests/lifecycle.rs</files>
  <action>Tambahkan test crash-restart: spawn fake backend, kill paksa (SIGKILL) tanpa stop graceful, spawn ulang dan pastikan handshake kedua berhasil tanpa error address-in-use pada level supervisor (port ephemeral colon-nol selalu dipilih ulang) — ini regresi langsung Pitfall #2. Tambahkan test ensure_data_dirs yang membuat database/ dan media/ di bawah direktori base sementara (paritas main.js userData layout), idempoten bila sudah ada. Tambahkan test live-backend yang di-skip otomatis bila biner Go tidak ada: bila perintah go tersedia, build biner dari ./cmd/api ke direktori temp, spawn via Supervisor, tunggu Ready via probe nyata, lalu stop dan asersi no-orphan; bila go atau biner tidak tersedia, test mem-print alasan skip dan lolos (jangan gagal di CI tanpa Go). Pastikan seluruh test hermetik: tidak memakai port fixed, tidak menyentuh config dir asli.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo test -p wabot-supervisor --manifest-path desktop-gpui/Cargo.toml -- --nocapture</automated></verify>
  <done>Restart-loop hijau, data dirs idempoten, live-backend teruji bila Go ada atau skip beralasan bila tidak</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor→backend child | Spawn biner lokal dengan env terkontrol; stdout/stderr tak tepercaya (parse defensif) |
| CLI args→supervisor | Flag --no-backend --port dari operator lokal; validasi rentang port |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02a | Tampering | env_vars / CLI args | medium | mitigate | Secret hanya via env (tidak pernah argv); parse port ketat 1-65535 |
| T-01-02b | Denial of service | orphan backend / port race | high | mitigate | kill_on_drop + stop() eksplisit + adopt-or-kill + restart-loop test |
| T-01-02c | Information disclosure | stderr_tail / env di log | medium | mitigate | Log metadata saja (exit code, tail ringkas); tidak pernah log env secret atau payload |
</threat_model>

<verification>
cargo test -p wabot-supervisor hijau penuh; clippy -D warnings bersih; tidak ada orphan setelah suite.
</verification>

<success_criteria>
Kriteria sukses roadmap #2 terpenuhi: spawn + handshake + kill bersih tanpa orphan, termasuk crash-restart; mode --no-backend paritas Electron.
</success_criteria>

<output>
Create `.planning/phases/01-fondasi-workspace-supervisor/01-02-SUMMARY.md` when done
</output>
