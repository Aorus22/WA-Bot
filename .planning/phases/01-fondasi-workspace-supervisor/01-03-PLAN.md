---
phase: 01-fondasi-workspace-supervisor
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - desktop-gpui/crates/settings/Cargo.toml
  - desktop-gpui/crates/settings/src/lib.rs
  - desktop-gpui/crates/settings/src/paths.rs
  - desktop-gpui/crates/settings/tests/persistence.rs
  - .github/workflows/desktop-gpui.yml
autonomous: true
requirements:
  - CORE-03
  - CORE-05
estimate:
  tokens: 50000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Preferensi UI tersimpan di OS config dir dan bertahan antar restart
    - File korup tidak merusak startup (fallback default + backup)
    - CI matrix Linux + Windows hijau dari fondasi
  artifacts:
    - desktop-gpui/crates/settings/src/lib.rs
    - desktop-gpui/crates/settings/src/paths.rs
    - .github/workflows/desktop-gpui.yml
  key_links:
    - Settings load/save -> OS config dir (dirs crate) -> bertahan antar restart
    - CI workflow -> cargo build + test di ubuntu + windows -> gap platform tertangkap dini
---

<objective>
Crate `wabot-settings` menyimpan preferensi UI di OS config dir (termasuk trio kunci tema web) plus CI matrix Linux + Windows hijau dari fondasi. Mencegah Pitfall #10-paruh-awal (platform gap telat) dan menegakkan batasan settings-hanya-UI.

Purpose: Preferensi (tema, window, backend override) harus persisten dan portabel; pengaturan DB-backed milik server tidak boleh diduplikasi lokal. CI dua OS dari hari pertama mencegah kejutan packaging.
Output: Crate settings teruji + workflow CI yang dipakai semua phase berikutnya.
</objective>

<execution_context>
@/home/aorus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/aorus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-fondasi-workspace-supervisor/01-CONTEXT.md
@.planning/phases/01-fondasi-workspace-supervisor/01-01-SUMMARY.md
@/home/aorus/project/web-term/desktop-gpui/crates/settings/src/lib.rs
@/home/aorus/project/web-term/desktop-gpui/crates/settings/src/paths.rs
@/home/aorus/project/wa-bot/web/src/components/AppThemeProvider.tsx
@/home/aorus/project/wa-bot/web/src/App.tsx
</context>

<tasks>

<task type="tracer">
  <name>Tracer: round-trip settings save->load->persist</name>
  <files>desktop-gpui/crates/settings/src/lib.rs, desktop-gpui/crates/settings/src/paths.rs, desktop-gpui/crates/settings/tests/persistence.rs</files>
  <action>Implementasi crate wabot-settings (UI prefs ONLY — anti-pattern ARCHITECTURE: jangan duplikasi kunci DB-backed server seperti Gemini key/TTS ke JSON lokal; itu tetap via GET/PUT /api/settings di Phase 7). Struct DesktopSettings (serde JSON) berisi: theme_preset String (default preset gelap web; semantik nama sama dengan kunci wa-bot-theme-preset agar kompatibel Phase 3), theme_mode enum System/Light/Dark (default System; memetakan kunci wa-bot-theme dan wa-bot-theme-mode), backend_path_override Option PathBuf, last_backend_url Option String, window_state (x/y/width/height/maximized). Modul paths memakai dirs config_dir dengan direktori wa-bot-desktop (BUKAN webterm-desktop), fungsi with_base yang injectable untuk test hermetik, plus ensure_data_dirs yang membuat database/ dan media/ (paritas main.js userData layout). Fungsi load/save: load hilang berarti default + save; save atomic via tulis-temp-lalu-rename. Test tracer: round-trip penuh save→load→nilai sama→restart-simulasi (load ulang dari disk) di direktori base sementara; tidak menyentuh config dir asli.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo test -p wabot-settings --manifest-path desktop-gpui/Cargo.toml</automated></verify>
  <done>Round-trip persist hijau; skema kunci tema kompatibel dengan tiga kunci web (wa-bot-theme, wa-bot-theme-mode, wa-bot-theme-preset)</done>
</task>

<task type="auto">
  <name>Corrupt-file recovery + validasi + batasan UI-only</name>
  <files>desktop-gpui/crates/settings/src/lib.rs, desktop-gpui/crates/settings/tests/persistence.rs</files>
  <action>Perkeras crate dari tracer: load menghadapi JSON korup harus me-rename file rusak ke settings.json.corrupt (dengan timestamp bila memungkinkan, tanpa panic) lalu kembali ke default dan tetap bisa save normal — uji dengan menulis bytes sampah lalu load dan asersi default + file backup ada. Tambahkan validasi: theme_preset asing justru dipertahankan apa adanya (forward-compat dengan preset baru web, JANGAN reject), window bounds negatif dinormalisasi ke None, backend_path_override relatif ditolak menjadi None. Tambahkan doc-comment batasan di header lib.rs yang eksplisit melarang field secret/setting server (daftar: AI keys, TTS, read-receipts — milik backend) agar phase mendatang tidak salah menaruh. Uji unknown-field tolerance: JSON dengan field ekstra tak dikenal harus tetap ter-load (serde default, bukan deny_unknown_fields) karena skema web bisa bertambah.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo test -p wabot-settings --manifest-path desktop-gpui/Cargo.toml && cargo clippy -p wabot-settings --manifest-path desktop-gpui/Cargo.toml -- -D warnings</automated></verify>
  <done>File korup pulih ke default dengan backup; unknown fields toleran; clippy bersih</done>
</task>

<task type="auto">
  <name>CI matrix Linux + Windows dari fondasi</name>
  <files>.github/workflows/desktop-gpui.yml</files>
  <action>Buat workflow GitHub Actions desktop-gpui.yml yang TIDAK mengganggu workflow docker-build.yml yang ada: trigger push/pull_request dengan paths filter desktop-gpui/**, rust-toolchain.toml, dan file workflow itu sendiri. Jobs: build-test dengan strategy matrix os ubuntu-latest + windows-latest, steps checkout, instal stable toolchain via rust-toolchain (dapur rust-lang/rustup action atau rustup langsung), cargo build --workspace --locked (flag locked WAJIB agar lockfile drift menggagalkan CI — penegakan pin eksak), cargo test --workspace --locked, dan cargo check --target x86_64-pc-windows-gnu di job ubuntu sebagai jaring pengaman cross (tandai continue-on-error false; bila gagal karena toolchain env, biarkan gagal terlihat — jangan sembunyikan). Tambahkan job pin-audit ringan: grep memastikan tidak ada pin longgar di workspace (pola version tanpa equals) — gagalkan bila ada. Hindari macOS runner apa pun (out of scope milestone). Verifikasi YAML valid dan matrix memuat kedua OS.</action>
  <verify><automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/desktop-gpui.yml')); m=d['jobs']['build-test']['strategy']['matrix']['os']; assert 'ubuntu-latest' in m and 'windows-latest' in m, m; print('MATRIX_OK', m)" && grep -q -- '--locked' .github/workflows/desktop-gpui.yml && echo LOCKED_OK</automated></verify>
  <done>Workflow ada, YAML valid, matrix memuat ubuntu + windows, semua langkah cargo memakai --locked</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings-file→app | JSON di config dir dapat diedit user/rusak; parse defensif + backup |
| CI→registry | Fetch crates.io di runner; --locked mencegah drift tak disengaja |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03a | Tampering | settings.json korup/asing | low | mitigate | Corrupt→backup+default; unknown fields toleran; bounds dinormalisasi |
| T-01-03b | Information disclosure | secret server di settings lokal | medium | mitigate | Batasan UI-only terdokumentasi + hanya tiga kunci tema + path/URL/window; tanpa secret |
| T-01-03c | Tampering | dep drift di CI | medium | mitigate | cargo --locked di semua langkah + job pin-audit grep |
</threat_model>

<verification>
cargo test -p wabot-settings hijau; clippy bersih; workflow YAML valid dengan matrix dua OS dan --locked.
</verification>

<success_criteria>
Kriteria sukses roadmap #3 (preferensi bertahan antar restart termasuk file korup) dan #1 sisi CI (matrix Linux + Windows hijau) terpenuhi.
</success_criteria>

<output>
Create `.planning/phases/01-fondasi-workspace-supervisor/01-03-SUMMARY.md` when done
</output>
