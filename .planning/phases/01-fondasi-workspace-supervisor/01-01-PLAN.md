---
phase: 01-fondasi-workspace-supervisor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain.toml
  - desktop-gpui/Cargo.toml
  - desktop-gpui/Cargo.lock
  - desktop-gpui/crates/supervisor/Cargo.toml
  - desktop-gpui/crates/supervisor/src/lib.rs
  - desktop-gpui/crates/settings/Cargo.toml
  - desktop-gpui/crates/settings/src/lib.rs
  - desktop-gpui/crates/backend-client/Cargo.toml
  - desktop-gpui/crates/backend-client/src/lib.rs
  - desktop-gpui/crates/app/Cargo.toml
  - desktop-gpui/crates/app/src/main.rs
autonomous: true
requirements:
  - CORE-01
  - CORE-05
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Developer bisa `cargo build --workspace` hijau di Linux dari fondasi
    - Semua dependensi inti ter-pin eksak dengan Cargo.lock ter-commit
    - Pasangan gpui-pre 0.3.3 + gpui-component 0.6.0 terbukti kompil bersama (tanpa E0277)
  artifacts:
    - rust-toolchain.toml
    - desktop-gpui/Cargo.toml
    - desktop-gpui/Cargo.lock
    - desktop-gpui/crates/app/src/main.rs
  key_links:
    - Workspace Cargo.toml exact pins -> Cargo.lock ter-commit -> CI mengonsumsi lockfile yang sama
    - Crate app bergantung pada gpui-pre + gpui-component berpasangan
---

<objective>
Workspace Cargo `desktop-gpui/` ter-scaffold dengan pin versi eksak + Cargo.lock ter-commit + seluruh member crate ter-build hijau di Linux. Ini fondasi yang mencegah Pitfall #1 (gpui churn).

Purpose: Tanpa workspace ter-pin dari hari pertama, setiap `cargo update` berisiko mematahkan build pre-1.0 GPUI berminggu kemudian. Plan ini mengunci reproduksibilitas sebelum kode apa pun ditulis.
Output: Workspace kompil hijau + toolchain terinstal + stub member untuk diisi plan 01-02/01-03 dan Phase 2.
</objective>

<execution_context>
@/home/aorus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/aorus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-fondasi-workspace-supervisor/01-CONTEXT.md
@/home/aorus/project/web-term/desktop-gpui/Cargo.toml
</context>

<tasks>

<task type="tracer">
  <name>Tracer: toolchain + workspace ter-build end-to-end</name>
  <files>rust-toolchain.toml, desktop-gpui/Cargo.toml, desktop-gpui/Cargo.lock, desktop-gpui/crates/*/Cargo.toml, desktop-gpui/crates/*/src/*.rs</files>
  <action>ASUMSI TEREKAM: environment ini TIDAK memiliki rustc/cargo/rustup (terverifikasi: rustc, cargo, rustup semuanya command not found). Instal Rust stable via rustup installer resmi (curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable), lalu source $HOME/.cargo/env di setiap perintah cargo berikutnya. Tulis rust-toolchain.toml yang me-pin channel stable (isi: [toolchain] channel="stable"). Buat workspace desktop-gpui/ dengan resolver 2 dan members crates/supervisor, crates/settings, crates/backend-client, crates/app, mengikuti pola ../web-term/desktop-gpui/Cargo.toml secara struktural (bukan copy buta): [workspace.package] version 0.1.0 edition 2021, [workspace.dependencies] dengan SEMUA pin eksak memakai sintaks equals (gpui package gpui-pre =0.3.3, gpui-component =0.6.0, gpui-platform package gpui-pre-platform =0.3.3 hanya untuk crate app, tokio =1.53.1 dengan features rt-multi-thread/process/macros/sync/time/net, reqwest =0.12.28 default-features false dengan json/multipart/rustls-tls, tokio-tungstenite =0.26.2 default-features false connect/handshake, futures-util =0.3.32, flume =0.12.0, serde =1.0.229 derive, serde_json =1.0.151, dirs =6.0.0, thiserror =2.0.20, anyhow =1.0.104, parking_lot =0.12.5, rand =0.10.2, tempfile =3.27.0 dev, libc =0.2.189 dev), plus [profile.release] lto="thin". Buat keempat member crate sebagai stub minimal yang kompil: supervisor (lib.rs dengan satu item pub agar crate tidak kosong), settings (lib.rs + paths.rs kosong ber-mod), backend-client (lib.rs stub — Phase 2 yang mengisi, JANGAN tulis DTO/REST/WS di sini), app (bin wabot dengan main.rs minimal). Nama package internal: wabot-supervisor, wabot-settings, wabot-backend-client, wabot. Jalankan cargo build --workspace untuk men-generate Cargo.lock, pastikan Cargo.lock ter-track git (cek .gitignore tidak mengecualikannya; bila tercantum, hapus baris itu). Hindari registry gpui 0.2.2 (snapshot basi pra-split) dan reqwest default-tls/OpenSSL (merusak build hermetik Linux).</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo build --workspace --manifest-path desktop-gpui/Cargo.toml && test -f desktop-gpui/Cargo.lock && git check-ignore desktop-gpui/Cargo.lock; test $? -ne 0</automated></verify>
  <done>Cargo build workspace hijau, Cargo.lock ada dan tidak di-ignore git, rust-toolchain.toml ada</done>
</task>

<task type="auto">
  <name>Buktikan pasangan gpui-pre + gpui-component kompil bersama</name>
  <files>desktop-gpui/crates/app/Cargo.toml, desktop-gpui/crates/app/src/main.rs</files>
  <action>Crate app bergantung pada gpui (package gpui-pre =0.3.3), gpui-component =0.6.0, gpui-platform (package gpui-pre-platform =0.3.3), dan anyhow. Tulis main.rs yang benar-benar menyentuh kedua API: panggil gpui_platform::application() untuk entry dan rujuk satu tipe dari gpui-component (misalnya Theme atau komponen root) agar linker/type-checker memaksa kedua crate di-resolve — stub main kosong TIDAK cukup karena tidak membuktikan pasangan matched (jebakan E0277: kedua sisi kompil terpisah tapi gagal di call site). Bump parsial dilarang: catat di komentar Cargo.toml bahwa gpui-pre dan gpui-component harus selalu di-bump bersamaan.</action>
  <verify><automated>source "$HOME/.cargo/env" && cargo build -p wabot --manifest-path desktop-gpui/Cargo.toml</automated></verify>
  <done>Crate wabot kompil dengan kedua crate GPUI ter-resolve; mismatch versi akan gagal di sini, bukan di phase mendatang</done>
</task>

<task type="auto">
  <name>Kunci pin eksak + siapkan asumsi Windows-target</name>
  <files>desktop-gpui/Cargo.toml</files>
  <action>Verifikasi setiap dependensi inti di workspace Cargo.toml memakai sintaks equals (=x.y.z) tanpa caret/tilde/wildcard — grep seluruh baris version di [workspace.dependencies] dan perbaiki yang longgar. Tambahkan target Windows x86_64-pc-windows-gnu via rustup target add; lalu coba cargo check --target x86_64-pc-windows-gnu --workspace. Bila cross-check gagal karena linker mingw tidak ada di environment ini, CATAT sebagai asumsi di SUMMARY (verifikasi Windows menjadi tanggung jawab CI di plan 01-03) dan jangan stall — keberhasilan plan ini ditentukan oleh build Linux host, bukan cross-check.</action>
  <verify><automated>source "$HOME/.cargo/env" && grep -E 'version\s*=\s*"' desktop-gpui/Cargo.toml | grep -v '=' | wc -l | grep -q '^0$' && echo PINS_OK; cargo metadata --format-version 1 --manifest-path desktop-gpui/Cargo.toml > /dev/null && echo METADATA_OK</automated></verify>
  <done>Semua pin memakai equals persis; metadata resolve sukses; status cross-check Windows tercatat (lolos atau CI-only)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine→crates.io | Fetch dependensi saat resolve; lockfile mengunci integritas antar mesin |
| toolchain-install→shell | rustup installer dieksekusi via curl-pipe; verifikasi TLS (--proto/=https --tlsv1.2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Cargo.lock / registry deps | medium | mitigate | Pin eksak + Cargo.lock ter-commit; upgrade hanya sebagai PR terisolasi |
| T-01-SC | Tampering | toolchain install via curl | medium | mitigate | Installer resmi rustup dengan flag TLS ketat; catat versi toolchain di SUMMARY |
</threat_model>

<verification>
cargo build --workspace hijau; Cargo.lock ter-commit; pasangan gpui terbukti kompil; pin equals semua.
</verification>

<success_criteria>
Kriteria sukses roadmap #1 (cargo build hijau) dan #4 (pin eksak + lockfile) terpenuhi di sisi fondasi workspace; CI hijau menyusul di plan 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-fondasi-workspace-supervisor/01-01-SUMMARY.md` when done
</output>
