---
phase: 02-backend-client-rust
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - desktop-gpui/crates/backend-client/src/multipart.rs
  - desktop-gpui/crates/backend-client/src/client.rs
  - desktop-gpui/crates/backend-client/tests/multipart.rs
autonomous: true
requirements:
  - CORE-04
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Builder multipart form untuk send-media sama field-for-field dengan web
    - Method upload media untuk foto grup dan status media didukung
    - Unit test memverifikasi struktur form multipart yang dihasilkan
  artifacts:
    - desktop-gpui/crates/backend-client/src/multipart.rs
    - desktop-gpui/crates/backend-client/tests/multipart.rs
  key_links:
    - HttpClient mengintegrasikan builder multipart untuk POST /api/send-media, /api/statuses/media, dan /api/groups/{id}/photo
---

<objective>
Mengimplementasikan builder multipart request ber-type untuk pengiriman media (`send-media`, `send-audio/ptt`, `set-group-photo`, `post-status-media`) dengan metadata lengkap (target, secret, type, caption, viewOnce, ptt, seconds, waveform).
</objective>

<tasks>
1. Buat `multipart.rs` dengan `SendMediaForm`, `StatusMediaForm`, dan `GroupPhotoForm`.
2. Integrasikan ke `HttpClient` (`send_media`, `send_audio`, `set_group_photo`, `post_status_media`) dan tulis unit tests di `tests/multipart.rs`.
</tasks>
