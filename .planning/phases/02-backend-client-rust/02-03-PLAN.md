---
phase: 02-backend-client-rust
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - desktop-gpui/crates/backend-client/src/ws.rs
  - desktop-gpui/crates/backend-client/src/lib.rs
  - desktop-gpui/crates/backend-client/tests/live_contract.rs
autonomous: true
requirements:
  - CORE-04
  - AUTH-05
estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - Client mengirim WS authenticate handshake di setiap koneksi terbuka
    - Auto-reconnect 3s berjalan saat close abnormal (bukan code 1000)
    - URL di-resolve ulang setiap kali reconnect
    - Event websocket diparsing ke WsEvent ber-type dan dipancarkan ke subscriber
    - Integration test memverifikasi round-trip terhadap backend asli jika Go tersedia
  artifacts:
    - desktop-gpui/crates/backend-client/src/ws.rs
    - desktop-gpui/crates/backend-client/tests/live_contract.rs
  key_links:
    - WsClient mengelola koneksi tokio-tungstenite dan reconnect loop
    - Tests live contract memvalidasi handshake dan health check REST terhadap Go backend
---

<objective>
Mengimplementasikan `WsClient` dan WebSocket event pump dengan authenticate handshake, reconnect loop ~3s pada close abnormal, dynamic URL re-resolution, parsing strongly-typed `WsEvent`, dan live contract round-trip tests melawan backend Go lokal.
</objective>

<tasks>
1. Buat `ws.rs` dengan `WsMessage`, `WsEvent`, `WsClient`, reconnect loop, authenticate handshake, dan channel subscription.
2. Re-export di `lib.rs`.
3. Buat live contract integration test di `tests/live_contract.rs` yang men-spawn backend Go atau fake server, memanggil endpoint REST, dan mengetes koneksi WebSocket.
</tasks>
